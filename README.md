TODO:

- language + sort option saved (in file, as key value -> some dicitonary class with enum)
- global error handler?
- left bar - books, settings (language)
- jump to up button
- Optimize (delete in background, on event delete in ui)
- normal size cover, bucket
- repair cover scaling in alert
- inspect button impl
- better styling (responsive)

- auth, login + password + name, Bookcase -> name Bookcase (new title) -> User entity, book -> add user field